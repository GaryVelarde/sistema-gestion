export enum userType {
    teacher = 'docentes',
    hotbed = 'semilleros',
    student = 'estudiantes',
  }
  
export enum eModule {
  advisory = 'asesorias',
  eventUdi = 'reunionesUdi',
  inscription = 'inscripcion',
  hotbed = 'semilleros',
  review = 'review',
}